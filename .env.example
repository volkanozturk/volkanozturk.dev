# The site reads all content from the repository — no CMS credentials are needed
# to build or deploy. Copy to .env.local only if you want the optional flag below.

# Show draft posts during local development (`npm run dev`).
# Production builds ignore this flag and always exclude drafts.
# INCLUDE_DRAFTS=1
